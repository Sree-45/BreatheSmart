# Pin servlet stack — webflux is on the classpath only for WebClient.
spring.main.web-application-type=servlet

google.maps.api.key=${GOOGLE_MAPS_API_KEY:replace-me}
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/breathesmart}
# RAG service (Python FastAPI)
rag.service.base-url=${RAG_SERVICE_BASE_URL:http://localhost:8000}

# JWT Settings
jwt.secret=${JWT_SECRET:replace-with-a-long-random-secret}
jwt.expiration=86400000

# Gemini AI Settings
gemini.api.key=${GEMINI_API_KEY:replace-me}
gemini.project.id=breathesmart-ai
gemini.location=us-central1
gemini.model.name=gemini-flash-latest

# Spring AI -> Gemini via OpenAI-compatible endpoint
# (Same Gemini API key works; no GCP credentials needed.)
spring.ai.openai.api-key=${GEMINI_API_KEY:replace-me}
spring.ai.openai.base-url=https://generativelanguage.googleapis.com/v1beta/openai
spring.ai.openai.chat.options.model=${gemini.model.name}
spring.ai.openai.chat.options.temperature=0.3

# File upload (health reports)
app.uploads.dir=${APP_UPLOADS_DIR:./uploads/reports}
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size=20MB